
      <%= form_for(word, :url => { :action => "toggle", :id => word.id, :controller => "vocs" },
                   html: { method: :post }, class: 'follow',
                   remote: true) do |f| %>
          <%= f.submit "forget", class: "forget" %>
      <% end %>



