<% if user_signed_in? %>
    <h5>Words you have remembered:</h5>
    <ul>
        <% current_user.words.each do |w| %>
            <li><%= w.text %><%= render :partial => 'home/forget', :locals => {:word => w} %><div class=clear></div></li>

        <% end %>
    </ul>

<% end %>